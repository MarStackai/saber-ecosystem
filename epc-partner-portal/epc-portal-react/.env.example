# Upstash Redis Configuration (Optional - for production)
# Sign up at https://upstash.com/ for free Redis instance
# UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-redis-token

# For development, the app will use in-memory storage fallback
# No Redis credentials needed for local development